export interface MarkdownTool {
  slug: string;
  metaTitle: string;
  metaDescription: string;
  toolTitle: string;
  explanation: string;
  defaultMarkdown: string;
}

export const markdownTools: MarkdownTool[] = [
  {
    slug: 'preview-github-readme-template',
    metaTitle: 'GitHub README Preview Online — Live Markdown Editor | SandboxMax',
    metaDescription:
      'Write and preview your GitHub README in real time with GitHub-dark styling — headers, code blocks, tables, and task lists. Free, no signup, 100% in your browser.',
    toolTitle: 'GitHub README Live Preview',
    explanation:
      'Pre-loaded with a complete README skeleton — title, feature bullets, fenced install commands, usage code, and a contributing checklist — rendered exactly the way GitHub\'s dark theme will show it. Edit on the left, watch the right pane keep up on every keystroke; nothing you write leaves this tab.',
    defaultMarkdown: [
      '# SandboxKit',
      '',
      '> Zero-friction utilities for people who ship. `v2.4.1` · MIT · **no telemetry**',
      '',
      'A tiny toolkit that does the boring parts of your build pipeline, locally.',
      '',
      '## Features',
      '',
      '- **Instant** — no daemon, no config file, no login',
      '- **Private** — everything runs on *your* machine',
      '- ~~Cloud sync~~ deliberately not a feature',
      '- Works with [any CI provider](https://sandboxmax.com)',
      '',
      '## Install',
      '',
      '```bash',
      'npm install -g sandboxkit',
      'sandboxkit init --preset minimal',
      '```',
      '',
      '## Usage',
      '',
      '```js',
      'import { kit } from "sandboxkit";',
      '',
      'const result = kit.run("./src", { fix: true });',
      'console.log(`fixed ${result.count} files`);',
      '```',
      '',
      '## Contributing',
      '',
      '- [x] Read the code of conduct',
      '- [x] Fork and create a feature branch',
      '- [ ] Add tests for your change',
      '- [ ] Open a pull request',
      '',
      '---',
      '',
      'Made with `markdown` — previewed on SandboxMax.',
    ].join('\n'),
  },
  {
    slug: 'format-markdown-table-matrix',
    metaTitle: 'Markdown Table Formatter & Preview — Column Alignment Live | SandboxMax',
    metaDescription:
      'Build and preview markdown tables with left, center, and right column alignment — rendered live in GitHub dark style. Free and fully client-side.',
    toolTitle: 'Markdown Table Matrix Formatter',
    explanation:
      'Pre-loaded with an API reference table using all three alignment syntaxes — :--- left, :---: center, ---: right — plus inline code and bold inside cells, the combination that breaks most naive renderers. Edit a colon in the separator row and watch the whole column snap to its new alignment.',
    defaultMarkdown: [
      '# API Endpoint Matrix',
      '',
      'Alignment is controlled by the colon positions in the separator row:',
      '',
      '| Method | Endpoint | Auth required | Latency (p95) |',
      '|:-------|:--------------:|:---:|----------:|',
      '| `GET` | `/v1/users` | no | 42 ms |',
      '| `POST` | `/v1/users` | **yes** | 118 ms |',
      '| `PATCH` | `/v1/users/:id` | **yes** | 97 ms |',
      '| `DELETE` | `/v1/users/:id` | **yes** + admin | 64 ms |',
      '',
      '## Alignment cheat sheet',
      '',
      '| Syntax | Result |',
      '|:---|:---|',
      '| `:---` | left-aligned column |',
      '| `:---:` | centered column |',
      '| `---:` | right-aligned column |',
    ].join('\n'),
  },
  {
    slug: 'render-technical-documentation-strings',
    metaTitle: 'Render Technical Documentation Markdown — Live Preview | SandboxMax',
    metaDescription:
      'Preview technical docs markdown with blockquotes, nested lists, task checklists, and inline formatting — GitHub dark styling, rendered live in your browser.',
    toolTitle: 'Technical Documentation Renderer',
    explanation:
      'Pre-loaded with the constructs technical writers actually use: warning blockquotes, nested bullet hierarchies, an interactive task checklist (click the checkboxes — they update the markdown source), and dense inline formatting. Everything renders in GitHub\'s dark documentation style as you type.',
    defaultMarkdown: [
      '## Deployment Runbook',
      '',
      '> **Warning:** always run the checklist below *before* promoting to production.',
      '> Skipping steps is how the `2026-03` incident happened.',
      '',
      '### Pre-flight checklist',
      '',
      '- [x] Migrations applied to staging',
      '- [x] Feature flags set to `off` by default',
      '- [ ] Load test passed at 2x expected traffic',
      '- [ ] On-call engineer confirmed',
      '',
      '### Rollout order',
      '',
      '1. Deploy the **API layer** first',
      '   - watch `error_rate` for 10 minutes',
      '   - rollback threshold: *0.5%*',
      '2. Deploy the web tier',
      '3. Flip the feature flag to `on` for 5% of users',
      '',
      '### Useful commands',
      '',
      '```bash',
      'kubectl rollout status deploy/api --watch',
      'kubectl rollout undo deploy/api   # the panic button',
      '```',
      '',
      'Questions? See the [escalation guide](https://sandboxmax.com) or ping `#deploys`.',
    ].join('\n'),
  },
];
