---
import Layout from '../../layouts/Layout.astro';
import SiteHeader from '../../components/SiteHeader.astro';
import SiteFooter from '../../components/SiteFooter.astro';
import ToolSteps from '../../components/ToolSteps.astro';
import UrlEncoder from '../../components/UrlEncoder.astro';
import { urlTools } from '../../data/urlTools';

export function getStaticPaths() {
  return urlTools.map((tool) => ({
    params: { slug: tool.slug },
    props: { tool },
  }));
}

const { tool } = Astro.props;
const related = urlTools.filter((t) => t.slug !== tool.slug);
---

<Layout title={tool.metaTitle} description={tool.metaDescription} toolName={tool.toolTitle}>
  <SiteHeader />

  <main class="mx-auto max-w-6xl px-4 py-10 sm:px-6">
    <nav class="mb-6 font-mono text-xs text-zinc-500">
      <a href="/" class="transition hover:text-zinc-300">sandboxmax</a>
      <span class="mx-1">/</span>
      <a href="/url/url-encode-decode-strings" class="transition hover:text-zinc-300">url</a>
      <span class="mx-1">/</span>
      <span class="text-zinc-400">{tool.slug}</span>
    </nav>

    <div class="mb-8 max-w-3xl">
      <h1 class="text-3xl font-extrabold tracking-tight text-zinc-50 sm:text-4xl">
        {tool.toolTitle}
      </h1>
      <p class="mt-3 leading-relaxed text-zinc-400">{tool.explanation}</p>
      <p class="mt-3 inline-flex items-center gap-2 rounded-full border border-emerald-400/25 bg-emerald-400/10 px-3 py-1 font-mono text-xs text-emerald-300">
        <span class="size-1.5 rounded-full bg-emerald-400"></span>
        runs 100% in your browser — URLs are parsed, never visited
      </p>
    </div>

    <ToolSteps toolName={tool.toolTitle} steps={[
          ['Paste URL or text', 'Any string works — full URLs unlock the parameter grid automatically.'],
          ['Encode or decode', 'One click either way; your last action keeps re-running live as you type.'],
          ['Work the param grid', 'Every query key decoded into rows, trackers flagged amber — copy a clean URL or the params as JSON.'],
        ]} />

    <UrlEncoder defaultDirection={tool.defaultDirection} defaultInput={tool.defaultInput} />

    {
      related.length > 0 && (
        <section class="mt-14">
          <h2 class="mb-4 text-lg font-bold text-zinc-100">More URL tools</h2>
          <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {related.map((t) => (
              <a
                href={`/url/${t.slug}`}
                class="group rounded-xl border border-zinc-800 bg-zinc-900/40 p-5 transition hover:border-indigo-400/40 hover:bg-zinc-900"
              >
                <span class="font-mono text-xs text-zinc-500">/url/{t.slug}</span>
                <h3 class="mt-1 font-semibold text-zinc-100 group-hover:text-indigo-300">{t.toolTitle}</h3>
              </a>
            ))}
          </div>
        </section>
      )
    }
  </main>

  <SiteFooter />
</Layout>
